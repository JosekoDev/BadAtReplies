# Copy to .env and adjust for your host
#   cp .env.example .env

# Match the host user that owns ./config (id -u / id -g)
PUID=1000
PGID=1000

# Timezone (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Etc/UTC

# Host ports mapped to the container
HTTP_PORT=3000
HTTPS_PORT=3001
